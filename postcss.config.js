module.exports = {
    plugins: {
        // include whatever plugins you want
        // but make sure you install these via yarn or npm!

        autoprefixer: {},
    },
    browserslist: [
        "> 0.5%",
        "last 2 versions",
        "Firefox ESR",
        "not dead",
        "iOS >= 15",
        "Chrome >= 100",
        "Edge >= 100",
        "Firefox >= 100",
        "Safari >= 15",
        "Samsung >= 22",
        "Opera >= 100",
    ],
}